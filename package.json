{
    "name": "{{ project-name }}",
    "version": "1.0.0",
    "description": "Worker template for a rewrite proxy",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "{{ authors }}",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0"
    }
}
